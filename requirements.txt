# Master rad — paralelizacija RL treniranja sa Ray RLlib
# Instaliraj: pip install -r requirements.txt

ray[rllib]>=2.40.0
gymnasium>=1.0.0
gymnasium[classic-control]>=1.0.0   # CartPole, LunarLander, Pendulum...
torch>=2.0.0
tensorboard>=2.14.0
matplotlib>=3.8.0
pandas>=2.1.0
pyyaml>=6.0
imageio>=2.33.0                     # Snimanje demo GIF-ova

# Za LunarLander (Box2D):
# pip install "gymnasium[box2d]"

# Za Atari Pong:
# pip install "gymnasium[atari]" ale-py
